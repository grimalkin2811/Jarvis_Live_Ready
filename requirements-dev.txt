# Outils de développement / build (pas nécessaires pour l'utilisateur final).
ruff>=0.5
pyinstaller>=6.0
# Note : la régénération des fixtures tests/fixtures/openwakeword/*.onnx
# (tests/_make_oww_stubs.py) demande `pip install onnx`, inutile sinon.
